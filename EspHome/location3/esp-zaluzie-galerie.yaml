# Configuration
substitutions:
  name: "esp-zaluzie-galerie"
  friendly_name: "Žaluzie - galerie"

packages:
  cover-template: !include ../templates/template-esp-sonoff-dual-r3-cover.yaml

wifi:
  use_address: esp-sonoff-dual-r3-12.local
