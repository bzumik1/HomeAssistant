# Configuration
substitutions:
  name: "esp-vypinac-satna"
  friendly_name: "Vypínač - šatna"
  # Relay trip limits
  max_power: "3600.0"
  max_temp: "95.0"

packages:
  common: !include templates/template-esp-shelly-2pm-event.yaml
  bluetooth-proxy: !include templates/bluetooth-proxy.yaml